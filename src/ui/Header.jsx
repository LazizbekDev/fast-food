import { Link } from "react-router-dom"

const Header = () => {
    return (
        <header>
            <Link to={'/'}>Pizza Co.</Link>

            <p>content</p>
        </header>
    )
}

export default Header;